import tkinterfrom tkinter import *import questionmaster = Tk()master.title("Roommate Match")master.geometry(("900X900"))#master.wm_iconbitmap("Troll_Face.ico")class Questionaire(Frame):    def __init__(self, master=None):        Frame.__init__(self, master)        self.row_count=5        self.answer_key = {}    def fill_in(self):        Label(master, text="Full Name").grid(row=0)        Label(master, text="Gender").grid(row=1)        Label(master, text="What state are you from?(Please enter state abbreviation").grid(row=2)        Label(master, text="Age").grid(row=3)        Label(master, text= "Leave a a personal message or a short description of yourself").grid(row=4)        e1 = Entry(master)        e2 = Entry(master)        e3 = Entry(master)        e4 = Entry(master)        e5 = Entry(master)        e1.grid(row=0, column=1)        e2.grid(row=1, column=1)        e3.grid(row=2, column=1)        e4.grid(row=3, column=1)        e5.grid(row=4, column=1)	           def createEntry(self,question):        Label(master,text=question).grid(row= self.row_count)        e = Entry(master)        e.grid(row=self.row_count+1,column=1)        self.row_count+=2    def create_yesno(self, question):        var= IntVar()        column=1        Label(master, text = question).grid(row = self.row_count)        Radiobutton(master, text="yes", variable= var,value=1).grid(row = self.row_count,column = column, sticky = W)        column+=1        Radiobutton(master, text="no", variable= var,value=0).grid(row = self.row_count,column = column, sticky = W)        self.answer_key[question] = var                self.row_count+=1    def multpliChoice(self, question):        var =IntVar()        Label(master, text = question).grid(row= self.row_count)    def multAnswr(self, answer):        letter = ord(a)        for i in range(len(answers)):            Radiobutton(self, text=answers[i], variable= var,value=i).grid(row= self.row_count,column= i+1)        self.answer_key[question] = var        self.row_count+=1def callback():    print("submit worked")Button(master, text= "Quit", command=master.quit).grid(row=23, sticky=W, pady=4)submit_button= Button(master, text= "submit", command= callback).grid(row=23, column=2)q_sheet = Questionaire()for i in question.fill_in().fq:	q_sheet.createEntry(i)for i in question.ynquestions().qs:	q_sheet.create_yesno(i)	#print(question.questions().qs)for i in question.multquestions().mq:	q_sheet.multpliChoice(i)master.mainloop()